/**
 * Bump this whenever the Community Rules / EULA text materially changes.
 * Any locally-saved profile whose `eulaVersion` doesn't match must re-consent
 * before reaching the main app (Guideline 1.2 — the gate must apply to
 * existing installs too, not just fresh onboarding).
 */
export const CURRENT_EULA_VERSION = 1;
